{
  "name": "@repo/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"(no build)\"",
    "typecheck": "echo \"(no typecheck)\"",
    "lint": "echo \"(no lint)\"",
    "test": "echo \"(no tests)\""
  }
}
